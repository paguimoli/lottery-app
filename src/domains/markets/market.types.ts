export type Market = {
  id: string;
  name: string;
  code: string;
  language: string;
  currency: string;
  timeZone: string;
  dateFormat: string;
  numberFormat: string;
  defaultBrand: string;
  active: boolean;
  createdAt: string;
};
